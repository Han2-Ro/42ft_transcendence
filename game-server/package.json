{
  "name": "game-server",
  "version": "1.0.0",
  "scripts": {
    "dev": "dotenv -e ../.env -- tsx watch server.ts",
    "build": "esbuild server.ts --bundle --platform=node --format=cjs --outfile=dist/server.js",
    "start": "node dist/server.js",
    "lint": "eslint"
  },
  "dependencies": {
    "shared": "workspace:*",
    "socket.io": "^4.7.0",
    "jsonwebtoken": "9.0.3",
    "@types/jsonwebtoken": "9.0.10"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
